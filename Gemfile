source "https://rubygems.org"
# To update Bundler, run `bundle update --bundler`
# To install dependencies, run `bundle install`
# To run Jekyll, run `bundle exec jekyll serve`
# To upgrade, run `bundle update github-pages`.
  gem "github-pages", group: :jekyll_plugins
# If you have any plugins, put them here!
group :jekyll_plugins do
  gem "jekyll-feed"
end

gem "jemoji"
# Emojis supported by GitHub Pages

gem "rouge"
# Syntax highlighter supported by GitHub Pages

gem "webrick", "~> 1.8"
